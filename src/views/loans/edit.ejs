<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Edit Loan <%= loan._id %>
    </title>
</head>

<body>
    <h1>Edit Loan <%= loan._id %>
    </h1>
    <form action="/loans/<%= loan._id %>" method="POST">
        <input type="hidden" name="_method" value="PUT">
        <label for="amount">Amount:</label>
        <input type="number" name="amount" id="amount" value="<%= loan.amount %>" required>
        <br>
        <label for="interestRate">Interest Rate:</label>
        <input type="number" name="interestRate" id="interestRate" value="<%= loan.interestRate %>" required>
        <br>
        <label for="term">Term:</label>
        <input type="number" name="term" id="term" value="<%= loan.term %>" required>
        <br>
        <label for="user">User:</label>
        <input type="text" name="user" id="user" value="<%= loan.user %>" required>
        <br>
        <input type="submit" value="Save Changes">
    </form>
    <a href="/loans">Back to Loans</a>
</body>

</html>